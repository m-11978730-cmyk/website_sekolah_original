(function () {
  const root = document.documentElement;
  const body = document.body;

  if (!body) return;

  root.style.cursor = 'url("axolot_cursor.png") 0 0, auto';
  body.style.cursor = 'url("axolot_cursor.png") 0 0, auto';
})();
